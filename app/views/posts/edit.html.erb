
<%= form_with(model: @post, local: true, html: {class: 'form-signin'}) do |form| %>
</br>
<h3>Edit Post</h3>

  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@post.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @post.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
  </br>
  <%= form.text_area :post_content, id: 'post_content', class: 'form-control' %>
</p>
</br>
<p>
  <%= form.submit "Update Post", class: 'btn btn-outline-dark btn-sm ml-auto' %>
</p>
<% end %>